fastapi==0.141.1
uvicorn[standard]==0.52.4
pillow==12.3.0
numpy==2.5.2
python-multipart==0.0.32
pydantic-settings==2.15.0

# CPU-only build - the GPU build is multiple GB and this service never needs CUDA.
# pip reads both index URLs and prefers the more specific match, so this line doesn't affect
# the packages above it.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
transformers==5.16.1

# Dev/test only
pytest==9.1.1
httpx==0.28.1
